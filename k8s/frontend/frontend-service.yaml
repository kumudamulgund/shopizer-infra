apiVersion: v1
kind: Service
metadata:
  name: shopizer-frontend
  namespace: shopizer
spec:
  selector:
    app: shopizer-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30000
  type: NodePort
