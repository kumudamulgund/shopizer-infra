apiVersion: v1
kind: Service
metadata:
  name: shopizer-admin
  namespace: shopizer
spec:
  selector:
    app: shopizer-admin
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30001
  type: NodePort
