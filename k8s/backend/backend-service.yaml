apiVersion: v1
kind: Service
metadata:
  name: shopizer-backend
  namespace: shopizer
spec:
  selector:
    app: shopizer-backend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30002
  type: NodePort
